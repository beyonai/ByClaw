## ── stage 1: build byclaw extensions ──────────────────────────────
FROM node:22-slim AS ext-builder

WORKDIR /build

COPY byclaw-exe/extensions/ ./src/

RUN npm config set registry https://registry.npmmirror.com --global

# install, build, prune, then assemble clean output into /build/out/
RUN set -eux; \
    mkdir -p /build/out; \
    for dir in src/*/; do \
        [ -f "$dir/package.json" ] || continue; \
        name=$(basename "$dir"); \
        echo ">>> npm install: $name"; \
        (cd "$dir" && npm install); \
        if node --input-type=module -e ' \
            import { readFileSync } from "fs"; \
            const pkg = JSON.parse(readFileSync(process.argv[1] + "package.json", "utf8")); \
            process.exit((pkg.scripts || {}).build ? 0 : 1); \
        ' "$dir"; then \
            echo ">>> npm run build: $name"; \
            (cd "$dir" && npm run build); \
        fi; \
        echo ">>> prune dev deps: $name"; \
        (cd "$dir" && npm prune --omit=dev); \
        target="/build/out/$name"; \
        mkdir -p "$target"; \
        [ -d "$dir/dist" ] && cp -r "$dir/dist" "$target/dist"; \
        [ -d "$target/dist" ] && find "$target/dist" -name '*.map' -delete; \
        for f in package.json openclaw.plugin.json; do \
            [ -f "$dir/$f" ] && cp "$dir/$f" "$target/"; \
        done; \
        [ -d "$dir/node_modules" ] && cp -r "$dir/node_modules" "$target/node_modules"; \
    done; \
    echo ">>> built extensions:"; ls -la /build/out/

FROM ghcr.io/astral-sh/uv:python3.12-bookworm-slim AS python312

FROM ghcr.io/tencentcloud/cubesandbox-base:2026.16 AS cubesandbox-base

## ── stage 2: final image ─────────────────────────────────────────
FROM ghcr.io/openclaw/openclaw:2026.4.21

USER root

RUN chown root:root /app
WORKDIR /app

ENV PIP_BREAK_SYSTEM_PACKAGES=1 \
    PIP_ROOT_USER_ACTION=ignore \
    ENVD_PORT=49983 \
    ENVD_LOG_FILE=/var/log/envd.log \
    OPENCLAW_GATEWAY_PORT=18789 \
    FILEBROWSER_PORT=8082 \
    FILEBROWSER_ROOT=/by \
    FILEBROWSER_BASEURL=/filebrowser \
    OPENCLAW_STATE_DIR=/by/.openclaw \
    DWS_CONFIG_DIR=/by/.openclaw/.dws

RUN set -eux; \
    if [ -f /etc/apt/sources.list.d/debian.sources ]; then \
        sed -i 's|http://deb.debian.org/debian|https://mirrors.tuna.tsinghua.edu.cn/debian|g' /etc/apt/sources.list.d/debian.sources; \
        sed -i 's|http://deb.debian.org/debian-security|https://mirrors.tuna.tsinghua.edu.cn/debian-security|g' /etc/apt/sources.list.d/debian.sources; \
    elif [ -f /etc/apt/sources.list ]; then \
        sed -i 's|http://deb.debian.org/debian|https://mirrors.tuna.tsinghua.edu.cn/debian|g' /etc/apt/sources.list; \
        sed -i 's|http://security.debian.org/debian-security|https://mirrors.tuna.tsinghua.edu.cn/debian-security|g' /etc/apt/sources.list; \
    fi; \
    printf 'Acquire::Retries "5";\n' > /etc/apt/apt.conf.d/80-retries; \
    apt-get update; \
    apt-get install -y --no-install-recommends \
        ca-certificates \
        curl \
        libreoffice \
        unzip \
        zip; \
    libreoffice --version; \
    rm -rf /var/lib/apt/lists/*

COPY --from=cubesandbox-base /usr/bin/envd /usr/bin/envd
COPY --from=cubesandbox-base /usr/local/bin/cube-entrypoint.sh /usr/local/bin/cube-entrypoint.sh
RUN chmod 0755 /usr/bin/envd /usr/local/bin/cube-entrypoint.sh

COPY --from=python312 /usr/local/bin/python3.12 /usr/local/bin/pip3.12 /usr/local/bin/
COPY --from=python312 /usr/local/lib/libpython3.12.so* /usr/local/lib/
COPY --from=python312 /usr/local/lib/python3.12 /usr/local/lib/python3.12

RUN set -eux; \
    ln -sf /usr/local/bin/python3.12 /usr/local/bin/python3; \
    ln -sf /usr/local/bin/python3.12 /usr/bin/python3; \
    python3 --version | grep -q 'Python 3.12'; \
    python3 -m pip config --global set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple; \
    python3 -m pip config --global set global.trusted-host pypi.tuna.tsinghua.edu.cn

COPY --from=ext-builder /build/out/ /app/extensions/

# add openclaw skills (overwrite same-name files with project content)
COPY middleware/openclaw/skills/ /app/skills/
COPY middleware/openclaw/runtime-tools/ /usr/local/lib/byclaw/runtime-tools/
RUN cd /app/skills/knowledge-collection/scripts \
    && npm ci --omit=dev --prefer-offline

# add filebrowser
COPY middleware/openclaw/linux-amd64-filebrowser/filebrowser /usr/local/bin/filebrowser
RUN chmod 0755 /usr/local/bin/filebrowser
COPY middleware/openclaw/filebrowser-ui-proxy.py /usr/local/bin/openclaw-filebrowser-ui-proxy
COPY middleware/openclaw/filebrowser-ui/ /usr/local/share/openclaw/filebrowser-ui/
RUN chmod 0755 /usr/local/bin/openclaw-filebrowser-ui-proxy

# add DingTalk workspace cli dws from the repository instead of downloading at build time
COPY middleware/openclaw/cli/dws /usr/local/bin/dws
RUN chmod 0755 /usr/local/bin/dws

# CubeSandbox runtime commands. cube-entrypoint.sh owns envd; these scripts
# only manage foreground/background business processes.
RUN set -eux; \
    printf '%s\n' \
      '#!/bin/sh' \
      'set -eu' \
      '' \
      'FILEBROWSER_PORT="${FILEBROWSER_PORT:-8082}"' \
      'FILEBROWSER_ROOT="${FILEBROWSER_ROOT:-/by}"' \
      'FILEBROWSER_BASEURL="${FILEBROWSER_BASEURL:-/filebrowser}"' \
      'FILEBROWSER_UPSTREAM_PORT="${FILEBROWSER_UPSTREAM_PORT:-18082}"' \
      '' \
      'mkdir -p "${FILEBROWSER_ROOT}"' \
      'filebrowser \' \
      '  --root "${FILEBROWSER_ROOT}" \' \
      '  --port "${FILEBROWSER_UPSTREAM_PORT}" \' \
      '  --address 127.0.0.1 \' \
      '  --noauth \' \
      '  --baseurl "${FILEBROWSER_BASEURL}" &' \
      'exec python3 /usr/local/bin/openclaw-filebrowser-ui-proxy \' \
      '  --root "${FILEBROWSER_ROOT}" \' \
      '  --port "${FILEBROWSER_PORT}" \' \
      '  --address 0.0.0.0 \' \
      '  --baseurl "${FILEBROWSER_BASEURL}" \' \
      '  --upstream "http://127.0.0.1:${FILEBROWSER_UPSTREAM_PORT}" \' \
      '  --ui-dir /usr/local/share/openclaw/filebrowser-ui \' \
      '  --default-path "${FILEBROWSER_ROOT}" \' \
      '  --allowed-roots "${FILEBROWSER_ROOT}"' \
      >/usr/local/bin/startFilebrowser.sh; \
    printf '%s\n' \
      '#!/bin/sh' \
      'set -eu' \
      '' \
      'OPENCLAW_GATEWAY_PORT="${OPENCLAW_GATEWAY_PORT:-18789}"' \
      'OPENCLAW_GATEWAY_TOKEN="${OPENCLAW_GATEWAY_TOKEN:-dev}"' \
      'OPENCLAW_STATE_DIR="${OPENCLAW_STATE_DIR:-/by/.openclaw}"' \
      'DWS_CONFIG_DIR="${DWS_CONFIG_DIR:-${OPENCLAW_STATE_DIR}/.dws}"' \
      '' \
      'mkdir -p "${OPENCLAW_STATE_DIR}" "${DWS_CONFIG_DIR}"' \
      'if command -v git >/dev/null 2>&1 && [ -x /usr/local/lib/byclaw/runtime-tools/git-credential.mjs ]; then git config --global credential.https://github.com.helper /usr/local/lib/byclaw/runtime-tools/git-credential.mjs; fi' \
      'exec node dist/index.js gateway \' \
      '  --bind=lan \' \
      '  --port="${OPENCLAW_GATEWAY_PORT}" \' \
      '  --allow-unconfigured \' \
      '  --verbose \' \
      '  --token="${OPENCLAW_GATEWAY_TOKEN}"' \
      >/usr/local/bin/startOpenclaw.sh; \
    printf '%s\n' \
      '#!/bin/sh' \
      'set -eu' \
      '' \
      '/usr/local/bin/startFilebrowser.sh &' \
      'exec /usr/local/bin/startOpenclaw.sh' \
      >/usr/local/bin/startAll.sh; \
    chmod 0755 /usr/local/bin/startFilebrowser.sh /usr/local/bin/startOpenclaw.sh /usr/local/bin/startAll.sh

# Keep the default image command template-friendly: cube-entrypoint starts envd
# for CubeSandbox's 49983 /health probe, then starts OpenClaw and filebrowser.
EXPOSE 49983 18789 8082
HEALTHCHECK --interval=3m --timeout=10s --start-period=15s --retries=3 \
  CMD node -e "const ports=[process.env.OPENCLAW_GATEWAY_PORT,'8080','18789'].filter((port,index,all)=>port&&all.indexOf(port)===index);Promise.any(ports.map(async(port)=>{const response=await fetch('http://127.0.0.1:'+port+'/healthz');if(!response.ok)throw new Error('unhealthy')})).then(()=>process.exit(0)).catch(()=>process.exit(1))"
ENTRYPOINT ["/usr/local/bin/cube-entrypoint.sh"]
CMD ["/usr/local/bin/startAll.sh"]
