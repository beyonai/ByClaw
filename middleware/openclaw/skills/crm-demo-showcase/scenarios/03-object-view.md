# 对象与视图演示

用于解释对象和视图的区别，遵循上级 SKILL.md 的授权及资源挂载规则。

1. 查询当前数字员工已挂载的 OBJECT/VIEW 资源，展示实际名称与编码。
2. 根据用户选择，对已有 VIEW 执行自然语言查询，展示返回的数据。
3. 说明 OBJECT 提供业务数据操作，VIEW 提供查询；仅在用户明确要求写入时操作 OBJECT。
4. 缺少对应资源时提示用户在资源管理中创建或导入并授权，再继续演示。

报告包含已使用的资源、实际查询结果和简短说明。禁止调用建模脚本或编造创建结果。
