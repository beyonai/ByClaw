# examples

Sample code, minimal demos, and integration sketches kept separate from production apps.

- Do not ship real secrets; document variables in `.env.example` only.
- Optional layout: `examples/fe-plugin/`, `examples/api-client/`, etc.
