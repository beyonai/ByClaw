/**
 * Inlined at build time by esbuild (`--loader:.md=text`). Runtime only needs `dist/index.js`.
 * Source of truth: `templates/main-agents.md`.
 */
import mainAgentsMd from "../templates/main-agents.md";

export const BUILTIN_MAIN_AGENTS_MD: string = mainAgentsMd;
