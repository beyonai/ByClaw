{
  "name": "byai-channel",
  "version": "1.0.0",
  "description": "ByAI Channel - HTTP Webhook + Redis SDK channel with configurable streaming support",
  "type": "module",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/index.js --sourcemap --packages=external --external:openclaw --external:minio && esbuild src/channel-plugin-api.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/channel-plugin-api.js --sourcemap --packages=external --external:openclaw --external:minio && esbuild src/runtime-setter-api.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/runtime-setter-api.js --sourcemap --packages=external --external:openclaw --external:minio",
    "test": "vitest run"
  },
  "dependencies": {
    "@byclaw/by-framework": "^1.3.1",
    "ioredis": "^5.3.2",
    "uuid": "^9.0.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.0",
    "vitest": "^3.0.5"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ],
    "channel": {
      "id": "byai-channel",
      "label": "ByAI Channel",
      "selectionLabel": "ByAI Channel (HTTP + Redis SDK)",
      "docsPath": "/channels/byai-channel",
      "docsLabel": "byai-channel",
      "blurb": "HTTP webhook + Redis SDK channel with configurable streaming output for web integration.",
      "order": 86
    }
  }
}
