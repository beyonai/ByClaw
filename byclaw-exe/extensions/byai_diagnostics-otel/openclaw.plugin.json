{
  "id": "diagnostics-otel",
  "name": "BYAI Diagnostics OpenTelemetry",
  "description": "OpenClaw diagnostics OpenTelemetry exporter (BYAI Langfuse session/user mapping).",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "contentLimits": {
        "type": "object",
        "additionalProperties": false,
        "description": "Limits for content captured into OTEL span attributes.",
        "properties": {
          "maxAttributeChars": {
            "type": "number",
            "description": "Maximum characters per captured content attribute. Defaults to 131072."
          },
          "maxArrayItems": {
            "type": "number",
            "description": "Maximum items captured from content arrays. Defaults to 200."
          }
        }
      }
    }
  }
}
