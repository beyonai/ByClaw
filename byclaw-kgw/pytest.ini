[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = src
markers =
    integration: marks tests requiring real OpenGauss/Redis/MinIO (run with: pytest -m integration)
addopts = --strict-markers -m "not integration"
