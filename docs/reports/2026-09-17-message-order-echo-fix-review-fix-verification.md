# 《消息回显乱序》审查修复后的补充验证结论

| 项 | 值 |
|---|---|
| 缺陷单 | #234 |
| 仓库 / 分支 | `beyonai/ByClaw` · `fix/message-order-echo-234` |
| 本文档目的 | 澄清「测试报告 ↔ 被测 commit」映射，消除 PR head 前移后可能产生的歧义 |
| 补充验证对象 | `e4b2292a`（审查意见处置提交） |
| **结论** | ✅ **通过** |

---

## 1. 交付链路与 commit 映射

| # | 阶段 | commit | 内容 | 验证 |
|---|---|---|---|---|
| 1 | 缺陷修复 | `d835a025` | 消息列表在增量写入路径补齐「时间线升序」不变量 | T3 独立端到端验证 **通过 12/12**（报告：`docs/reports/2026-09-17-message-order-echo-fix-test-report.html`） |
| 2 | 测试报告入库 | `9d308c33` | 上述报告自包含化（截图内联）并脱敏入仓 | 内容保真校验：归一化资产引用后与源报告逐字符一致 |
| 3 | 审查意见处置 | `e4b2292a` | 处置 AI Code Review 的 7 条意见（性能 / 纯度 / 时间戳契约 / 测试健壮性） | 本文件所记录的**补充验证** |
| 4 | 本文档 | （本文件所在提交） | 记录链路、范围与结论 | — |

**映射声明**

- 库内那份 HTML 测试报告的校验对象是 **`d835a025`**，属**历史证据**，为保持证据可信性**不予改动**。
- 当前分支 head 为 **`e4b2292a`**（在 `d835a025` 之上增加了审查意见处置）。该 head 的验证结论**由本文档覆盖**。
- 因此：**HTML 报告 ≠ 最新 head 的验证书**；两者是链路中不同阶段的产物，请勿混用。

---

## 2. 本次补充验证的范围与限界

### 2.1 覆盖范围

| 维度 | 内容 | 结果 |
|---|---|---|
| 排序不变量 | 4 类场景：迟到旧消息重排、连续两次增量、多 lane 并发到达、迟到子会话投影 | **4/4 通过** |
| 纯度 / 不可变性 | 排序函数不就地修改入参；更新列表 reducer 不就地改写旧 state / 旧数组；子会话投影不就地修改入参 | **3/3 通过** |
| 排序契约 | 与源码 JSDoc 声明的 4 条契约**逐条交叉核对**（新数组 / 同刻按 `messageId` 升序 / 混合时间戳类型与缺失值处理 / 稳定且幂等） | **3/3 通过，无分歧** |
| 反证有效性 | 2 组缺陷注入，确认关键测试具备判别力、非空转 | **2/2 有效** |
| UI 定向回归 | **含**（1 轮）—— 因本次提交触及 reducer 逻辑，按流程执行 UI 轮 | **通过**：store 顺序 == 可视顺序 |

> 不变量与契约的核对由验证方**独立实现键函数**完成，未复用开发者的测试文件作为唯一证据。

### 2.2 限界与取舍

| 项 | 说明 |
|---|---|
| **UI 轮为何要做** | 本次提交不止改测试：它**触及了 reducer 逻辑**（见 §3），属行为变更，因此不适用「仅测试改动可免 UI」的豁免。 |
| **为何不复跑全量用例** | ① 既有 HTML 报告的 12 条用例针对 `d835a025`，已通过；② 全量回归与类型检查基线已在前一阶段给出并记录（全量用例通过数净增 7、类型检查与基线一致），重复执行不产生新信息；③ 本轮定位为**有界补充验证**，只针对差异面取证。 |
| **UI 轮的方式** | 复用**既有会话**（不新建会话、不向共享库写入消息）；通过在应用层向运行时 store 分派乱序消息（配合网络层人为延迟，使「到达时刻」与 `createTime` 解耦）来验证归位行为。 |
| **未覆盖** | 见 §4。 |

---

## 3. 两 commit 的差异性质及其对既有「12/12」结论的影响

### 3.1 `d835a025` → `e4b2292a` 的差异

| 文件 | 变更量 | 性质 |
|---|---|---|
| `byclaw-fe/src/models/useMessageStore.ts` | +15 / −4 | **触及 reducer 逻辑**：① 为排序函数补全契约 JSDoc；② `updateSessionMessageList` 新增**非数组兜底**——当 updater 返回非数组（`undefined`/`null`）时保留旧列表，而非让排序工具把列表静默清空。**排序算法本身未改**（仍为「先 `createTime` 升序、同刻按 `messageId` 升序」）。 |
| `byclaw-fe/src/models/__tests__/useMessageStore.test.ts` | +157 | **仅测试**：新增 7 条回归/契约/边界用例，并为既有子会话投影用例补充断言。 |
| `docs/reports/2026-09-17-message-order-echo-fix-test-report.html` | 无 | 本次提交**未改动**（由上一提交引入）。 |

### 3.2 对既有「12/12 通过」结论的影响判断

**结论：不影响，12/12 仍然成立。**

理由（基于验证证据，而非推断）：

1. **被验证的行为未变**——`e4b2292a` 没有修改排序算法本身；排序键、比较逻辑与调用点与 `d835a025` 一致。
2. **新增分支不进入正常路径**——非数组兜底仅在 updater 返回违反契约的异常值时才生效；既有报告中 12 条用例的正常与边界路径均不触发该分支，因此其判定结果不变。
3. **差异面已被独立覆盖**——`e4b2292a` 由本轮补充验证直接覆盖：不变量、纯度、契约三维度全部通过，且 UI 轮确认「store 顺序 == 可视顺序」，即排序行为在真实运行时保持一致。
4. **两条证据互补而非替代**——`d835a025` 的 12/12 覆盖端到端场景；`e4b2292a` 的补充验证覆盖差异面与契约一致性。二者合起来才构成最新 head 的完整验证依据。

> 如需将 `e4b2292a` 也纳入端到端 12 条用例的直接验证，可另开任务；本轮按有界范围执行，未扩面。

---

## 4. 未覆盖项与剩余风险

| # | 项 | 等级 | 说明 |
|---|---|---|---|
| U1 | 真实多代理并发流本身未被「网络层真实乱序」触发 | 🟡 中 | UI 轮的乱序是通过应用层向运行时 store 分派注入的（配合网络延迟）；注入点与缺陷点同作用域，但不等价于真实多子会话并发链路的到达交错。建议后续用真实多代理任务抽检。 |
| U2 | 未新建会话，未走「新会话 + 真实流式」 | 🟢 低 | 为避免向共享环境写入，刻意复用既有会话；新会话路径的 UI 表现未在本轮重新验证（既有 HTML 报告中已覆盖）。 |
| U3 | 未重跑全量用例 / 全量类型检查 | 🟢 低 | 仅执行目标文件的用例、格式与静态检查；全量回归与类型检查基线已在前一阶段产生并记录，本轮不重复。 |
| U4 | 性能结论基于合成 benchmark | 🟢 低 | 典型会话规模下单次排序耗时远低于流式刷新间隔；**真实长会话 + 高频流式**未压测。 |
| U5 | 秒级时间戳假设未加防御 | 🟢 低 | 当前数据为毫秒级；若后端将来下发秒级时间戳，排序会整体前移。该假设已写入源码契约注释。 |
| U6 | `FilePicker.test.tsx` 为**既有基线失败** | 🟡 中 | 与本次改动无关，但会阻断本仓库的提交钩子。建议另开任务修复。 |
| U7 | 目标分支为 `develop` 时 CI 测试矩阵未覆盖 | 🟢 低 | 仓库 CI 配置仅对主要维护分支触发；本 PR 与本次验证的证据**全部来自本地执行**。 |
| U8 | 截图像素未做图像级脱敏 | 🟢 低 | 已入库的 HTML 报告为**字符级**脱敏；若截图内渲染了内部标识，需另行图像处理。 |
| — | 共享环境副作用 | — | 本轮未新建会话、未向共享库写入消息；后端以禁用后台 worker 的方式启动。 |

---

## 5. 证据与出处

| 项 | 说明 |
|---|---|
| 端到端验证报告（历史证据，验证 `d835a025`） | `docs/reports/2026-09-17-message-order-echo-fix-test-report.html` |
| 审查意见处置说明 | PR #235 描述「Review 意见处置」章节 + 同 PR 汇总评论 |
| 本补充验证的详细原始输出 | 存于会话交付物（含独立用例、反证注入、UI 轮日志与截图），**未入仓**；本文件为其**轻量结论记录** |
| 本文档合规性 | 不含沙箱绝对路径、工号、姓名、预览链接、令牌/密钥、内网地址 |

---

*本文档为轻量说明性记录，不替代上游验证报告；如有冲突，以上游报告的原始输出为准。*
